// @flow

import React, { PropTypes } from 'react';
const { number } = PropTypes;

export default class HorizontalGroup extends React.Component {
  static propTypes = {
    width: number
  };
  static defaultProps = {};

  render() {
    return (

    )
  }
}
